# 塔筒生产进度管控系统 — 启动指南

> 适用版本：前后端分离式单体（Vue3 + FastAPI + MySQL 8.0）
> 本文基于仓库实际代码整理，所有命令均对应真实文件结构。

---

## 1. 系统架构与请求链路

```
浏览器 (:5173)
   │  Vue3 + Vite 开发服务器
   │  axios baseURL = "/api"
   ▼
Vite 代理 (/api → http://localhost:8000)
   ▼
FastAPI 后端 (:8000)
   │  backend/app（core/db + routers + services，业务逻辑自包含）
   ▼
MySQL (:3306, 库名 tower_production)
```

- 后端端口 **8000**，前端开发端口 **5173**
- 前端无需单独配置后端地址：`vite.config.js` 已把 `/api` 代理到 `http://localhost:8000`
- 业务核心逻辑（`judge_node_status` 状态机、前序联动、按分组独立保存、Excel 解析）已内聚到 `backend/app/services/`（business_logic / workday_calendar / excel_parser / schedule_import），不再依赖旧 utils

---

## 2. 环境要求

| 组件 | 版本要求 | 说明 |
|------|---------|------|
| Python | 3.10+ | 运行后端（推荐 3.12） |
| Node.js | 18+ | 运行前端（推荐 20 / 22） |
| MySQL | 8.0 | 需 **已安装并正在运行**，默认端口 3306 |
| pip / npm | 随上述版本 | 依赖安装 |

> ⚠️ 本项目 **不内置 MySQL**，需你本机先装好 MySQL 并启动服务。

---

## 3. 第一步：准备 MySQL 数据库（最容易漏的一步）

后端 `init_database()` **只建表、不建库**，且连接时已指定库名 `tower_production`。因此必须**先建库**，否则任何数据库请求都会 500。

### 3.1 创建数据库
用任意 MySQL 客户端（命令行 / Workbench）执行：
```sql
CREATE DATABASE IF NOT EXISTS tower_production
  CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
```

### 3.2 建表（导入表结构）
在 **`tower_production_system/` 项目根目录**下执行：
```powershell
cd tower_production_system
$env:MYSQL_PASSWORD = "123456"          # 改成你的 root 密码
python -c "from database import init_database; init_database()"
```
正常输出：`[DB] Database initialized: tower_production`
（若已建过表，会提示 `already exists`，可重复执行，安全。）

> 说明：`init_database()` 内部用 `CREATE TABLE IF NOT EXISTS`，**不会清空已有数据**。
> 需先安装 `pymysql`：`pip install pymysql`（或直接用第 4 步建好的后端 venv）。
> 表清单：`projects / processes / process_node_plans / node_actual_progress / node_exceptions（异常提报）/ milestones / daily_progress 等`（见 `db_schema_mysql.sql`）。
> ⚠️ 仅首次建库会执行 schema；库已存在时不重建新表——若升级后缺少 `node_exceptions` 表，请从 `db_schema_mysql.sql` 复制其 CREATE TABLE 手动执行一次（或按 README「异常提报模块」SQL 创建）。

---

## 4. 第二步：启动后端（FastAPI :8000）

```powershell
cd tower_production_system\backend

# 1) 建虚拟环境（首次）
python -m venv .venv
.venv\Scripts\Activate.ps1              # 若报执行策略错误，先执行：
                                         #   Set-ExecutionPolicy -Scope Process RemoteSigned

# 2) 装依赖
pip install -r requirements.txt

# 3) 设置数据库连接（PowerShell 当前会话临时生效）
$env:MYSQL_HOST     = "127.0.0.1"
$env:MYSQL_PORT     = "3306"
$env:MYSQL_USER     = "root"
$env:MYSQL_PASSWORD = "123456"          # 改成你的 root 密码
$env:MYSQL_DATABASE = "tower_production"

# 4) 启动（--reload 改代码自动重启）
uvicorn app.main:app --reload --port 8000
```

启动成功后终端会出现类似：
```
INFO:     Uvicorn running on http://127.0.0.1:8000
INFO:     Application startup complete.
```

### 验证后端
- 交互式 API 文档：浏览器打开 **http://localhost:8000/docs**
- 健康检查：
  ```powershell
  curl http://localhost:8000/api/health
  # 期望返回：{"status":"ok","service":"tower-production-api"}
  ```

> 💡 想让环境变量**永久生效**（每次新开终端都有），改用：
> ```powershell
> [Environment]::SetEnvironmentVariable("MYSQL_PASSWORD", "123456", "User")
> ```
> 其他 4 个变量同理。

---

## 5. 第三步：启动前端（Vite :5173）

新开一个 **PowerShell 终端**（后端那个别关）：

```powershell
cd tower_production_system\frontend
npm install          # 首次安装依赖（需 Node 18+）
npm run dev
```

启动成功后终端提示：
```
  VITE v5.x  ready in xxx ms
  ➜  Local:   http://localhost:5173/
```

打开 **http://localhost:5173** 即可使用系统。前端所有 `/api` 请求会自动代理到后端 8000，无需额外配置。

---

## 6. 第四步：导入业务数据（推荐）

建好库表后项目列表是空的，需要先导入数据：

- **前端方式**：在页面上传「月度调度令」Excel（走 `POST /api/projects/{pid}/import-schedule`）
- **或直接调接口**：参考 `backend/README.md` 的 API 端点表

---

## 7. 环境变量速查

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `MYSQL_HOST` | `localhost` | MySQL 主机 |
| `MYSQL_PORT` | `3306` | MySQL 端口 |
| `MYSQL_USER` | `root` | 用户名 |
| `MYSQL_PASSWORD` | `""` | **必须设置**，否则连不上库 |
| `MYSQL_DATABASE` | `tower_production` | 库名 |

> PowerShell 设置：`$env:变量名 = "值"`
> bash / Linux 设置：`export 变量名="值"`

---

## 8. 常见坑与排错（实战记录）

| 现象 | 原因 | 解决 |
|------|------|------|
| `cd backend` 找不到路径 | `backend` 在 `tower_production_system\` 下，不在项目根 | `cd tower_production_system\backend` |
| `export: 无法识别` | PowerShell 不认 bash 的 `export` | 改用 `$env:MYSQL_PASSWORD="123456"` |
| 后端返回 **500** | 多为数据库连接 / `database.py` 导入失败 | 看后端终端 **traceback**；确认 MySQL 在跑、库已建、`pymysql` 已装 |
| 表不存在报错 | 未执行 `init_database()` | 按第 3 步建库 + 建表 |
| 前端请求一直转圈 | 后端没启动 / 端口不对 | 先确认 `curl http://localhost:8000/api/health` 正常 |
| venv 激活被拒 | PowerShell 执行策略 | `Set-ExecutionPolicy -Scope Process RemoteSigned` |

> 已知修复：原 `database.py` 硬依赖 `import streamlit`，在纯后端环境下会导致导入失败、所有 DB 接口 500。当前版本已将其改为「可选导入」（缺失时缓存降级为空操作），并修正了 `db.py` 的函数调用方式，**无需再安装 streamlit 即可运行后端**。

---

## 9. 停止与重启

- 停止：在对应终端按 **Ctrl+C**
- 后端已加 `--reload`，改 Python 代码会**自动重载**，无需手动重启
- 前端改代码由 Vite HMR 热更新

---

## 10. 快捷启动清单（按顺序）

```powershell
# —— 终端 A：MySQL 已在运行，先建库建表（仅需一次）——
cd tower_production_system
$env:MYSQL_PASSWORD="123456"
python -c "from database import init_database; init_database()"

# —— 终端 B：后端 ——
cd tower_production_system\backend
.venv\Scripts\Activate.ps1
$env:MYSQL_PASSWORD="123456"
uvicorn app.main:app --reload --port 8000

# —— 终端 C：前端 ——
cd tower_production_system\frontend
npm run dev
# 浏览器打开 http://localhost:5173
```
