# 塔筒系统 · 进度填报增加「手动选择填报日期」+ 权限控制（AI 修改提示词）

> 系统：Vue3 + Pinia + Element Plus + Vue Router（前端）/ FastAPI + MySQL（后端）。
> 技术栈与既定规范见 working memory：页面背景 `#f4f6f9`、卡片白底 `#ffffff`、边框 `#e2e8f0`、主色深蓝 `#1a365d`；语义色 完成绿 `#38a169` / 进行中蓝 `#3182ce` / 逾期红 `#e53e3e` / 未开始灰 `#718096`。
> 模式：纯前端预设账号权限（与 `store/auth.js` 一致），后端暂不做登录态强校验（二期项，见「待确认」）。

---

## 一、目标（Goal）

在「工序详情 / 填报进度」弹窗（`ProcessDetailDialog.vue`）中新增一个**填报日期选择器**：

1. 默认值为**当天**；授权用户（管理员）可手动改为历史日期或今日，用于补录/校正进度，使 `report_date` 真实反映"该进度属于哪一天"，从而更精准地追溯与控制延期与提前完成。
2. **权限控制**：当前登录用户角色为 `normal`（普通用户）时，日期选择器**禁用并锁定为当天**（不可编辑）；仅 `admin`（管理员）可使用自定义日期。
3. 日期选择器与现有"四分组手风琴 + 保存"表单自然融合，排版清晰、交互合理，不破坏既有状态判定与前序联动校验。

---

## 二、禁止改动（Do NOT change）

- **数据库表结构**：`node_actual_progress` 表及其 `report_date` 字段**已存在**，禁止新增/修改表或字段；禁止改动 `db_schema_mysql.sql`。
- **状态机**：`backend/app/services/business_logic.py` 的 `judge_node_status`、`split_node_groups`、`validate_today_quota` **函数签名与判定逻辑保持不变**（分组 today/overdue/future/done 仍按真实 `date.today()` 计算）。
- **四分组保存契约**：`saveNodeProgress` 仍按 `group` 独立保存、互不覆盖；`values: [{node_id, qty}]` 结构不变，仅**新增可选字段 `report_date`**。
- **`upsert_node_actual`**（`database.py`）：已支持任意 `report_date` 写入，禁止改写其逻辑，仅调用方传入新值。
- **`ScheduleImport` / 导入排产**：不受影响，禁止改动。
- **`store/auth.js` 的账号与 getter（`isAdmin` / `canEdit`）**：保持不变，仅消费，不修改预设账号。

---

## 三、修改点（Changes）

### 修改点 1 ｜ 前端弹窗加日期选择器（核心）

**文件**：`frontend/src/components/ProcessDetailDialog.vue`

- `<script setup>` 中引入 `useAuthStore`，新增 `reportDate` ref（默认今天，格式 `YYYY-MM-DD`）与 `fmtToday()` 辅助函数。
- 在「填报进度」模式块（即 `v-if="innerMode === 'input'"` 内、`el-segmented` 上方）插入一行：**标签「填报日期」+ `el-date-picker`（type="date", value-format="YYYY-MM-DD"）+ 提示文案**。
- 日期选择器 `:disabled="!auth.isAdmin"`：普通用户禁用并锁定今天；管理员可改。
- 打开弹窗（watch 的 open 分支）时 `reportDate` 重置为今天。
- `save()` 时把 `report_date: reportDate.value` 一并提交。

### 修改点 2 ｜ 前端 API 透传（无需改文件，仅说明调用契约）

**文件**：`frontend/src/api/node.js`
- `saveNodeProgress(pid, processName, payload)` 已 `http.post(..., payload)` 透传，无需改动；调用处（修改点 1 的 `save()`）把 `report_date` 放入 payload 即可。

### 修改点 3 ｜ 后端 Schema 增加可选字段

**文件**：`backend/app/schemas/__init__.py`
- `SaveNodeProgressRequest` 增加 `report_date: Optional[str] = None`。

### 修改点 4 ｜ 后端落库使用所选日期

**文件**：`backend/app/routers/nodes.py`
- 校验 `report_date` 格式（`YYYY-MM-DD`），非法则 400；为空则用 `today`。
- 循环写入时把原来的 `report_date = today.strftime(...)` 改为使用前端传入（或默认的）`report_date`。

---

## 四、代码（Code）

### 4.1 `frontend/src/components/ProcessDetailDialog.vue`

**模板：在 `v-if="innerMode === 'input'"` 块内、`el-segmented` 之前插入日期行**（仅展示新增片段，其余模板保持原样）：

```html
<!-- 填报模式（四分组手风琴） -->
<template v-if="innerMode === 'input'">
  <!-- ★ 新增：填报日期选择器（普通用户禁用并锁定今天） -->
  <div class="report-date-row">
    <span class="report-date-label">📅 填报日期</span>
    <el-date-picker
      v-model="reportDate"
      type="date"
      value-format="YYYY-MM-DD"
      format="YYYY-MM-DD"
      placeholder="选择填报日期"
      :disabled="!auth.isAdmin"
      :clearable="false"
      style="width: 200px;"
    />
    <span class="report-date-hint" :style="{ color: auth.isAdmin ? '#3182ce' : '#718096' }">
      {{ auth.isAdmin
          ? '可手动选择历史/今日日期进行补录'
          : '（普通账号仅可填报今日，日期已锁定）' }}
    </span>
  </div>

  <el-segmented v-model="activeGroup" :options="groupOptions" block style="margin: 14px 0;" />
  <!-- …… 以下 groupNodes / 保存按钮 等保持原样 …… -->
</template>
```

**脚本 `<script setup>` 顶部引入与新增状态**（在现有 `import { useProjectStore } ...` 附近追加）：

```js
import { useAuthStore } from '../store/auth'
const auth = useAuthStore()   // 权限：isAdmin 决定日期选择器是否可编辑

// ★ 填报日期：默认今天，格式 YYYY-MM-DD
function fmtToday() {
  const d = new Date()
  const p = (n) => String(n).padStart(2, '0')
  return `${d.getFullYear()}-${p(d.getMonth() + 1)}-${p(d.getDate())}`
}
const reportDate = ref(fmtToday())
```

**`save()` 函数**：在 payload 中追加 `report_date`（替换原 `saveNodeProgress(...)` 调用体）：

```js
async function save() {
  saving.value = true
  try {
    const values = groupNodes.value.map((n) => ({ node_id: n.id, qty: inputValues.value[n.id] ?? 0 }))
    const res = await saveNodeProgress(props.pid, props.processName, {
      group: activeGroup.value,
      values,
      report_date: reportDate.value,   // ★ 始终带上，后端按此落库（普通用户锁定为今天）
    })
    ElMessage.success(res.message)
    emit('saved')          // 父组件关闭弹窗 + 刷新
  } finally {
    saving.value = false
  }
}
```

**watch 打开分支**：重置日期为今天（保证每次打开都从当天开始，普通用户即使曾打开也不会残留旧值）：

```js
watch(
  () => [props.modelValue, props.processName, props.mode],
  ([open]) => {
    if (open) {
      innerMode.value = props.mode || 'detail'
      activeGroup.value = 'today'
      selectedNodeId.value = null
      reportDate.value = fmtToday()   // ★ 新增：重置填报日期为今天
      load()
    }
  },
)
```

**`<style scoped>` 追加样式**（贴合既有配色）：

```css
.report-date-row {
  display: flex;
  align-items: center;
  gap: 12px;
  background: #f7fafc;
  border: 1px solid #e2e8f0;
  border-radius: 8px;
  padding: 10px 14px;
  margin-bottom: 4px;
}
.report-date-label { font-size: 14px; font-weight: 600; color: #1a365d; white-space: nowrap; }
.report-date-hint { font-size: 12px; }
```

### 4.2 `backend/app/schemas/__init__.py`

```python
from typing import Optional
# （文件顶部已 import Optional；若无则补 import）

class SaveNodeProgressRequest(BaseModel):
    """按分组保存节点实际进度（分组独立，互不覆盖）。"""
    group: str
    values: list[NodeValue]
    report_date: Optional[str] = None   # ★ 新增：填报日期 YYYY-MM-DD；为空则用服务端当天
```

### 4.3 `backend/app/routers/nodes.py`

在 `save_node_progress` 函数内，将原来的：

```python
    today = date.today()
    groups = split_node_groups(proc_nodes, actuals, today)
```

与

```python
    report_date = today.strftime("%Y-%m-%d")
    saved = 0
    for p in groups[req.group]:
        qty = int(input_values.get(p["id"], actuals.get(p["id"], {}).get("actual_qty", 0)))
        db.upsert_node_actual(pid, p["id"], process_name, qty, report_date)
        saved += 1
```

改为（**新增格式校验 + 使用前端传入日期**）：

```python
    today = date.today()
    groups = split_node_groups(proc_nodes, actuals, today)

    # ★ 填报日期：前端传入则使用，否则回退当天；校验格式
    report_date = req.report_date or today.strftime("%Y-%m-%d")
    if req.report_date:
        try:
            datetime.strptime(req.report_date, "%Y-%m-%d")
        except (ValueError, TypeError):
            raise HTTPException(status_code=400, detail="report_date 格式必须为 YYYY-MM-DD")

    saved = 0
    for p in groups[req.group]:
        qty = int(input_values.get(p["id"], actuals.get(p["id"], {}).get("actual_qty", 0)))
        db.upsert_node_actual(pid, p["id"], process_name, qty, report_date)
        saved += 1
```

并在文件顶部 `from datetime import date` 改为 `from datetime import date, datetime`。

> 说明：后端按既有"纯前端校验"模型**信任前端传入的 `report_date`**（因当前无后端登录态）。权限的真实闸门在**前端日期选择器 `:disabled="!auth.isAdmin"`** + 打开时重置为今天。由此普通用户提交的 `report_date` 恒为当天，管理员可补录历史日期。

---

## 五、验收（Acceptance）

1. **默认行为**：以任意账号打开「填报进度」，日期选择器默认显示当天；保存后 `node_actual_progress.report_date` = 当天。
2. **权限-普通用户**：以 `user/user123`（role=normal）登录，日期选择器**置灰不可点**，提示"普通账号仅可填报今日，日期已锁定"；提交后落库 `report_date` = 当天，无法改为历史日期。
3. **权限-管理员**：以 `admin/admin123`（role=admin）登录，日期选择器可编辑；改为某历史日期后保存，落库 `report_date` = 所选日期；时间轴/节点详情中该进度按所选日期记录。
4. **融合度**：日期行位于四分组手风琴上方，配色与卡片风格一致；`el-segmented` 分组切换、`maxOf` 上限、前序联动校验（`validate_today_quota`）均不受影响。
5. **健壮性**：后端对非法 `report_date` 返回 400；不传该字段时回退当天，旧调用方（不含该字段）仍正常。
6. **回归**：`ScheduleImport` 排产导入、异常提报、三页月份联动等既有功能无变化。

---

## 六、待确认（Open Questions）

1. **后端强校验（二期）**：当前权限闸门在前端。若要求"即使直接调 API，普通角色也不能写入非今天日期"，需引入后端登录态（token/角色），在 `save_node_progress` 中按角色拒绝自定义 `report_date`。是否本次一并做？（当前按"纯前端校验"模型不做。）
2. **"as-of 评估"语义**：本次仅把所选日期作为 `report_date` 落库（审计/补录用途），状态机与分组仍按真实 `date.today()` 计算。若希望"所选日期"同时作为延期/提前完成的**评估基准日**（即按补录日期重算节点状态），需把 `as_of` 参数透传进 `judge_node_status`/`split_node_groups`，属更大改动，建议单列需求。
